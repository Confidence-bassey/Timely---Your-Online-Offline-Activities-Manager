<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MindfulMixOnlineOfflineActivitiesManager" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="FloatingActionButtonStyle" parent="Widget.Design.FloatingActionButton">
        <item name="android:layout_margin">8dp</item>
        <item name="android:layout_gravity">end|bottom</item>
        <item name="android:elevation">6dp</item>
    </style>


    <style name="Theme.MindfulMixOnlineOfflineActivitiesManager" parent="Base.Theme.MindfulMixOnlineOfflineActivitiesManager" />
</resources>